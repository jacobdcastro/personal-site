[build]
  command = "bun run build"
  publish = "dist/client"
[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888

# vanity path for the PDF resume
[[redirects]]
  from = "/resume"
  to = "/resume.pdf"
  status = 302
